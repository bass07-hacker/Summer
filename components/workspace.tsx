'use client'

import { useState } from 'react'
import { CalendarDays, Check, ChevronRight, CircleDollarSign, MapPin, Plus, Send, Sparkles, Users } from 'lucide-react'

const starterTrips = [{ name: 'Dakar, slowly.', place: 'Senegal · Aug 15–22', progress: 40, budget: '$2,840 / $6,000', status: 'Planning' }, { name: 'The long weekend', place: 'Lisbon · Oct 04–07', progress: 72, budget: '$1,120 / $2,000', status: 'Ready' }]

export function Workspace() {
  const [trips, setTrips] = useState(starterTrips)
  const [active, setActive] = useState(0)
  const [showForm, setShowForm] = useState(false)
  const [newName, setNewName] = useState('')
  const [newPlace, setNewPlace] = useState('')
  const [saved, setSaved] = useState(false)
  const trip = trips[active]

  function createTrip(event: React.FormEvent) { event.preventDefault(); if (!newName || !newPlace) return; setTrips([{ name: newName, place: newPlace, progress: 12, budget: '$0 / $0', status: 'Planning' }, ...trips]); setActive(0); setNewName(''); setNewPlace(''); setShowForm(false) }

  return <div className="rounded-[2rem] border border-border bg-card p-5 shadow-sm md:p-8"><div className="flex flex-col justify-between gap-5 md:flex-row md:items-start"><div><div className="mb-3 inline-flex items-center gap-2 rounded-full bg-brand/10 px-3 py-1.5 text-xs font-semibold text-brand"><Sparkles size={13} /> Your travel desk</div><h2 className="text-3xl font-semibold tracking-tight">Good plans start here.</h2><p className="mt-2 text-muted-foreground">Bring your people in, then let the trip take shape.</p></div><button onClick={() => setShowForm(!showForm)} className="rounded-full bg-brand px-5 py-3 text-sm font-semibold text-brand-foreground"><Plus className="mr-1 inline" size={16} /> New trip</button></div>{showForm && <form onSubmit={createTrip} className="mt-6 grid gap-3 rounded-2xl bg-muted p-4 md:grid-cols-[1fr_1fr_auto]"><input value={newName} onChange={e => setNewName(e.target.value)} placeholder="Trip name" className="rounded-xl border border-border bg-card px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand/30" /><input value={newPlace} onChange={e => setNewPlace(e.target.value)} placeholder="Destination + dates" className="rounded-xl border border-border bg-card px-4 py-3 text-sm outline-none focus:ring-2 focus:ring-brand/30" /><button className="rounded-xl bg-foreground px-5 py-3 text-sm font-semibold text-background">Create</button></form>}<div className="mt-8 grid gap-8 lg:grid-cols-[.8fr_1.2fr]"><div className="space-y-3">{trips.map((item, index) => <button key={`${item.name}-${index}`} onClick={() => setActive(index)} className={`w-full rounded-2xl border p-4 text-left transition ${active === index ? 'border-brand bg-brand/5' : 'border-border bg-background hover:border-brand/40'}`}><div className="flex items-start justify-between"><div><p className="font-semibold">{item.name}</p><p className="mt-1 text-sm text-muted-foreground">{item.place}</p></div><ChevronRight size={18} className="text-muted-foreground" /></div><div className="mt-4 h-1.5 rounded-full bg-muted"><div className="h-1.5 rounded-full bg-accent" style={{width: `${item.progress}%`}} /></div><div className="mt-2 flex justify-between text-xs text-muted-foreground"><span>{item.progress}% ready</span><span>{item.status}</span></div></button>)}<button onClick={() => setShowForm(true)} className="flex w-full items-center justify-center gap-2 rounded-2xl border border-dashed border-border p-4 text-sm font-medium text-muted-foreground hover:border-brand hover:text-brand"><Plus size={16} /> Add another trip</button></div><div className="rounded-2xl bg-background p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-muted-foreground">Active trip</p><h3 className="mt-1 text-2xl font-semibold">{trip.name}</h3></div><span className="rounded-full bg-accent/15 px-3 py-1.5 text-xs font-semibold text-accent">{trip.status}</span></div><div className="mt-6 grid gap-3 sm:grid-cols-3"><Metric icon={<CalendarDays size={17} />} label="Dates" value={trip.place.split(' · ')[1] || 'Add dates'} /><Metric icon={<CircleDollarSign size={17} />} label="Budget" value={trip.budget} /><Metric icon={<Users size={17} />} label="People" value="4 members" /></div><div className="mt-6 rounded-2xl border border-border bg-card p-4"><div className="flex items-center gap-3"><div className="flex size-9 items-center justify-center rounded-full bg-brand/10 text-brand"><MapPin size={17} /></div><div className="flex-1"><p className="text-sm font-semibold">Find a place to start</p><p className="text-xs text-muted-foreground">Save a stay, restaurant, or can&apos;t-miss moment.</p></div><button onClick={() => setSaved(true)} aria-label="Save a destination" className={`rounded-full p-2 ${saved ? 'bg-brand text-brand-foreground' : 'bg-muted text-foreground'}`}>{saved ? <Check size={16} /> : <Plus size={16} />}</button></div></div><div className="mt-3 grid grid-cols-2 gap-3"><button className="rounded-xl border border-border bg-card p-3 text-left text-sm"><Send className="mb-4 text-accent" size={17} /><span className="font-semibold">Invite your people</span><span className="mt-1 block text-xs text-muted-foreground">Share the link</span></button><button className="rounded-xl border border-border bg-card p-3 text-left text-sm"><CalendarDays className="mb-4 text-brand" size={17} /><span className="font-semibold">Build the days</span><span className="mt-1 block text-xs text-muted-foreground">Add a loose plan</span></button></div></div></div></div>
}
function Metric({icon,label,value}:{icon:React.ReactNode,label:string,value:string}) { return <div className="rounded-xl border border-border bg-card p-3"><div className="text-brand">{icon}</div><p className="mt-3 text-xs text-muted-foreground">{label}</p><p className="mt-1 truncate text-sm font-semibold">{value}</p></div> }
